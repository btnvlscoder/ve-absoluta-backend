# Configuración de Servidor
server.port=${PORT:8080}

# Configuración de Base de Datos - Neon (Cloud)
spring.datasource.url=jdbc:postgresql://ep-nameless-boat-anb786je.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_vIe0qprglMK8
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# 'update' permite que Hibernate cree o ajuste tablas automáticamente si detecta cambios en tus clases Kotlin 
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ============================================
# CLOUDINARY (Object Storage)
# ============================================

cloudinary.cloud-name=dd1bqevcx
cloudinary.api-key=281159852134971    
cloudinary.api-secret=8F0FHKKZsLrNyIOwq4zZNHkQcwo 

# Credenciales (usar variables de entorno en producción)
# minio.access-key=${MINIO_ACCESS_KEY}
# minio.secret-key=${MINIO_SECRET_KEY}

# URL de expiración para archivos privados (segundos)
minio.url-expiration=3600

# ============================================
# INFERENCE - Modo de inferencia
# ============================================
# Opciones: local (ProcessBuilder) | cloud (Hugging Face)
inference.mode=local

# Configuración cloud (descomentar cuando configures HF)
# inference.mode=cloud
# huggingface.endpoint=https://api-inference.huggingface.co/models/usuario/ve-absoluta
# huggingface.token=${HF_TOKEN}